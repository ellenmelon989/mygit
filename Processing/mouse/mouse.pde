void setup() {
  size (600, 400);
  background (0);
  frameRate (5);
}

void draw () {
  background(0);
}

void mousePressed() {
  circle (mouseX, mouseY, 15);
}//when any mouse button pressed- right, left, middle
